import { fontWeight, height, margin, width } from "@mui/system";

export default function Footer() {
  return (
    <div style={{
      color: "Grey",
      width: "0vh",
      margin: "0 auto"
    }}>footer</div>
  );
}

